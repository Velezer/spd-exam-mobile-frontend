# SPD Exam Mobile Frontend

### Build Command
```bash
eas build --profile preview --platform android
```

## Installation

1. Install dependencies:
```bash
npm install
```

2. Install EAS CLI (if not already installed):
```bash
npm install -g eas-cli
```

## Development

To run the development server:
```bash
npm start
```
